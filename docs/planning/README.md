# Planning 文档索引

## Status

Active.

## Purpose

`docs/planning/` 记录已确认阶段边界下的可执行任务拆分、依赖关系、验证路径和后续执行前置条件。

本目录不替代 `docs/phases/` 的阶段需求，也不替代 `docs/design/` 的系统设计。需求范围以 `docs/01-requirements.md` 和 `docs/phases/` 为准；实现任务边界以本目录的 planning 文档为准。

## Documents

- `00-phase0-poc-plan.md`：Phase0-PoC 唯一 active planning 文档，记录已完成的 P0-T3 trusted chain、当前 P0-T4 Draft generators 执行点和验证路径。
