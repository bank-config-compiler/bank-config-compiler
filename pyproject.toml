[project]
name = "bank-config-compiler"
version = "0.1.0"
description = "Phase0a PoC CLI for bank configuration compiler workspace artifacts."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyyaml>=6.0.3,<7",
]

[project.scripts]
bank-config-compiler = "bank_config_compiler.cli:main"

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
